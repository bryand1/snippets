#include <bits/stdc++.h>

typedef long double ld;
const int N = 20;

using namespace std;

ld p[N][N];

int main() { 
    cout.precision(10);
    cout << sizeof(ld) << endl;
    p[0][0] = 0.6800001L;
    cout << p[0][0] << endl;
    return 0;
}
